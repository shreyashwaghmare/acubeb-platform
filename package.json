{
  "name": "a-cube-b-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "backend"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "client": "cd apps/client-app && npx expo start",
    "build:client": "eas build --path apps/client-app --platform android --profile preview"
  }
}